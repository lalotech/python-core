## Module python-core

### Setup
Create local environment

> $ pipenv shell

Install dependencies

> $ pipenv install -r ./requirements/txt
> $ pipenv install

Add new dependency 

> $ pipenv install <new_package>

### Build

> $ pipenv run python -m build

### Use in others

> $ pipenv install ./mypackage-1.0.0.tar.gz
